name: Lint

on: # yamllint disable-line rule:truthy
  push:
    branches:
      - master
  pull_request:
    types: [opened, synchronize, reopened]

permissions: {}

jobs:
  build:
    name: Lint
    runs-on: ubuntu-latest

    permissions:
      contents: read
      packages: read
      # To report GitHub Actions status checks
      statuses: write

    steps:
      - name: Checkout code
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          # super-linter needs the full git history to get the
          # list of files that changed across commits
          fetch-depth: 0
          persist-credentials: false
      - name: Super-linter
        uses: super-linter/super-linter@9e863354e3ff62e0727d37183162c4a88873df41 # v8.6.0
        env:
          # To report GitHub Actions status checks
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          VALIDATE_ALL_CODEBASE: false
          VALIDATE_BIOME_FORMAT: false
          VALIDATE_BIOME_LINT: false
          VALIDATE_CHECKOV: false
          # C# / .NET linting is handled by the main build/test CI workflow;
          # the super-linter container does not include the .NET SDK so these
          # validators cannot run here.
          VALIDATE_CSHARP: false
          VALIDATE_DOTNET_SLN_FORMAT_ANALYZERS: false
          VALIDATE_DOTNET_SLN_FORMAT: false
          VALIDATE_DOTNET_SLN: false
          VALIDATE_JSCPD: false
          VALIDATE_TRIVY: false
          FIX_CSS_PRETTIER: false
          FIX_CSS: false
